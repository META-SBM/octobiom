% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retention_plot.R
\name{retention_plot}
\alias{retention_plot}
\title{Calculate and Visualize Taxa Retention Across Detection/Prevalence Thresholds}
\usage{
retention_plot(
  phyloseq,
  detection_values,
  prevalence_values,
  prevalence_colors = NULL
)
}
\arguments{
\item{phyloseq}{A phyloseq object containing microbiome data}

\item{detection_values}{Numeric vector of detection
thresholds to test (0-100 for percentages)}

\item{prevalence_values}{Numeric vector of prevalence
thresholds to test (0-1 for proportions)}

\item{prevalence_colors}{Character vector of colors for each prevalence level}
}
\value{
A ggplot object showing taxa retention patterns
}
\description{
This function evaluates how many microbial taxa are
retained under different combinations
of detection and prevalence thresholds, and visualizes
the results as a line plot.
}
\examples{
# Example with mock data
data(GlobalPatterns)
det_vals <- seq(0, 0.1, length.out = 5)
prev_vals <- seq(0.1, 0.9, by = 0.2)
colors <- viridis::viridis(length(prev_vals))
p <- retention_plot(
  phyloseq = GlobalPatterns,
  detection_values = det_vals,
  prevalence_values = prev_vals,
  taxonomic_level = "Genus",
  prevalence_colors = colors
)
print(p)
}
