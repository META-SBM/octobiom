% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{bootstrap_otu_table}
\alias{bootstrap_otu_table}
\title{Bootstrap OTU Table}
\usage{
bootstrap_otu_table(
  otu_table,
  sample_size = NULL,
  with_replacement = TRUE,
  random_seed = NULL
)
}
\arguments{
\item{otu_table}{A matrix or data.frame with samples as columns and OTUs as rows}

\item{sample_size}{Integer specifying the size of the bootstrap sample (default: NULL = same as original)}

\item{with_replacement}{Logical indicating whether to sample with replacement (TRUE) or without (FALSE)}

\item{random_seed}{Integer for reproducible random sampling (default: NULL)}
}
\value{
A bootstrapped OTU table with the same structure as input
}
\description{
Perform bootstrap resampling on an OTU table with options for sampling with or without replacement.
}
\examples{
# Create example OTU table
otu_data <- data.frame(
  Sample1 = c(10, 5, 3, 0),
  Sample2 = c(8, 2, 6, 1),
  Sample3 = c(15, 3, 0, 2),
  Sample4 = c(12, 4, 2, 0),
  Sample5 = c(9, 1, 5, 3),
  row.names = c("OTU1", "OTU2", "OTU3", "OTU4")
)

# Bootstrap with replacement
boot_with_repl <- bootstrap_otu_table(otu_data, sample_size = 5, random_seed = 42)

# Bootstrap without replacement
boot_without_repl <- bootstrap_otu_table(otu_data, sample_size = 3,

                                       with_replacement = FALSE, random_seed = 42)
}
